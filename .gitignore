# Build artifacts — the release zip is attached to GitHub Releases, never committed
/dist/
*.zip

# Logs from local Docker test runs
docker-logs/
*.log
test-run-*

# OS / editor noise
.DS_Store
Thumbs.db
*~
.idea/
.vscode/

# PHP tooling
/vendor/
.phpunit.result.cache
